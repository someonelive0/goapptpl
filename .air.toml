root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build  ./go-svc"
  bin = "go-svc.exe"
  delay = 1000 # ms
  exclude_dir = [ "tmp", ".git", "log" ]
  include_ext = ["go", "tpl", "toml", "html"]
  exclude_regex = ["_test\\.go"]

